import Link from 'next/link';
import { notFound } from 'next/navigation';
import { AppShell, Bars, Card, Empty, Heading, Notice, Stat } from '@/components/app-shell';
import { CategoryChart, MonthlyChart, TrendChart } from '@/components/charts';
import { Icon } from '@/components/icons';
import { farmContext, money, date, monthBounds, milkDailyTotals, number, sum } from '@/lib/farm';
import { createCostCenter, setAccountPassword } from '@/app/actions';
import { RegisterForm } from '@/components/register-form';
import { AssistantQuestions } from '@/components/assistant-questions';
export const dynamic='force-dynamic';
const allowed=['financeiro','rebanho','pesagens','leite','estoque','registrar','assistente','mais'];
const names:Record<string,string>={financeiro:'Financeiro',rebanho:'Rebanho',pesagens:'Pesagens',leite:'Leite',estoque:'Estoque',registrar:'Registrar',assistente:'Assistente IA',mais:'Mais'};
type Row=Record<string, string | number | boolean | null>;
const isDairy=(x:Row)=>x.sistema==='leite';
export default async function Area({params,searchParams}:{params:Promise<{area:string}>,searchParams:Promise<{erro?:string;salvo?:string}>}) {
 const {area}=await params;if(!allowed.includes(area))notFound();const flash=await searchParams;const {db,farm}=await farmContext();const id=farm.id;
 const load=async(table:string,order='criado_em')=>{const result=await db.from(table).select('*').eq('fazenda_id',id).order(order,{ascending:false}).limit(300);if(result.error)throw new Error(`Erro ao carregar ${table}`);return (result.data||[]) as Row[];};
 const shell=(child:React.ReactNode)=><AppShell farm={farm} active={area==='rebanho'?'Rebanho':area==='financeiro'?'Financeiro':area==='leite'?'Leite':area==='mais'?'Mais':''}><Heading eyebrow="Fazenda Varela IA · Gestão rural" title={names[area]}/><Notice error={flash.erro} ok={flash.salvo}/>{child}</AppShell>;
 if(area==='mais')return shell(<><div className="menu-list">{[['Pesagens','/painel/pesagens','scale'],['Estoque','/painel/estoque','box'],['Leite','/painel/leite','milk'],['Assistente IA','/painel/assistente','spark'],['Registrar','/painel/registrar','plus']].map(([name,href,icon])=><Link className="menu-item" key={href} href={href}><span><Icon name={icon} size={22}/></span><strong>{name}</strong><b>›</b></Link>)}</div><Card title="Acesso à conta"><p className="section-note">Defina uma senha para entrar diretamente, sem solicitar link por e-mail. Use uma senha exclusiva de pelo menos 12 caracteres. Você também pode continuar usando links.</p>{flash.salvo==='senha'&&<p role="status" className="app-notice good">Senha definida. Nas próximas vezes, entre com e-mail e senha.</p>}<form action={setAccountPassword} className="entry-form"><label>Nova senha<input name="password" type="password" autoComplete="new-password" minLength={12} required/></label><label>Confirmar senha<input name="confirmation" type="password" autoComplete="new-password" minLength={12} required/></label><button type="submit">Salvar minha senha</button></form></Card></>);
 if(area==='registrar'){const [lots,centers]=await Promise.all([load('lotes'),load('centros_custo')]);return shell(<RegisterForm lots={lots.map(x=>({id:String(x.id),nome:String(x.nome)}))} centers={centers.map(x=>({id:String(x.id),nome:String(x.nome)}))}/>);}
 if(area==='financeiro'){const [entries,centers]=await Promise.all([load('transacoes'),load('centros_custo')]);const {start,end,label}=monthBounds();const current=entries.filter(x=>String(x.data_competencia)>=start&&String(x.data_competencia)<end),income=current.filter(x=>x.tipo==='receita'),expense=current.filter(x=>x.tipo==='despesa'),r=sum(income,'valor'),d=sum(expense,'valor');const categories=Object.entries(expense.reduce<Record<string,number>>((obj,x)=>{const key=String(x.categoria);obj[key]=(obj[key]||0)+Number(x.valor);return obj;},{})).sort((a,b)=>b[1]-a[1]);return shell(<><p className="section-note">Competência · {label}</p><div className="stat-grid"><Stat label="Receitas" value={current.length?money(r):'—'}/><Stat label="Despesas" value={current.length?money(d):'—'}/><Stat label="Resultado" value={current.length?money(r-d):'—'}/><Stat label="Margem" value={r?`${number((r-d)/r*100,1)}%`:'—'}/></div><div className="two-col"><Card title="Receitas × despesas"><MonthlyChart months={Array.from({length:6},(_,i)=>{const dt=new Date();dt.setUTCDate(1);dt.setUTCMonth(dt.getUTCMonth()-(5-i));const key=dt.toISOString().slice(0,7);const subset=entries.filter(x=>String(x.data_competencia).startsWith(key));return {label:new Intl.DateTimeFormat('pt-BR',{month:'short'}).format(dt).replace('.',''),income:sum(subset.filter(x=>x.tipo==='receita'),'valor'),expense:sum(subset.filter(x=>x.tipo==='despesa'),'valor')};})}/></Card><Card title="Despesas por categoria"><CategoryChart items={categories}/></Card></div><Card title="Centros de custo">{centers.length?centers.map(x=><div className="data-row" key={String(x.id)}><strong>{String(x.nome)}</strong><span>{x.ativo?'Ativo':'Inativo'}</span></div>):<Empty title="Sem centros de custo" description="Separe custos por lote ou atividade da fazenda."/>}<form action={createCostCenter} className="inline-form"><input name="nome" placeholder="Nome do centro de custo" maxLength={120} required/><button>Criar centro</button></form></Card><Card title="Últimos lançamentos">{entries.length?entries.slice(0,20).map(x=><div className="data-row" key={String(x.id)}><div><strong>{String(x.descricao)}</strong><small>{date(String(x.data_competencia))} · {String(x.categoria)} · {String(x.status)}</small></div><b className={x.tipo==='despesa'?'negative':''}>{x.tipo==='despesa'?'-':'+'}{money(Number(x.valor))}</b></div>):<Empty title="Nenhum lançamento ainda" description="Registre uma receita ou despesa para acompanhar o caixa." href="/painel/registrar"/>}</Card></>);}
 if(area==='rebanho'){const [lotsRaw,animalsRaw,weighings,transactions]=await Promise.all([load('lotes'),load('animais'),load('pesagens','data_pesagem'),load('transacoes')]);const lots=lotsRaw.filter(x=>String(x.sistema||'corte')==='corte');const animals=animalsRaw.filter(x=>!isDairy(x));const active=animals.filter(x=>x.status==='ativo');return shell(<><section className="module-hero module-hero-beef"><span>GESTÃO DO GADO DE CORTE</span><h2>Cada animal, uma história de evolução.</h2><p>Lotes, pesagens e animais de corte em um só lugar.</p><Link href="/painel/novo/corte"><Icon name="plus" size={18}/> Adicionar animal de corte</Link></section><div className="module-quick-actions"><Link href="/painel/novo/lote" className="module-action secondary"><Icon name="plus" size={18}/> Criar lote</Link><Link href="/painel/novo/pesagem" className="module-action secondary"><Icon name="scale" size={18}/> Nova pesagem</Link></div><div className="stat-grid"><Stat label="Animais ativos" value={active.length}/><Stat label="Lotes ativos" value={lots.filter(x=>x.ativo).length}/><Stat label="Peso total conhecido" value={active.some(x=>x.peso_entrada!=null)?`${number(sum(active,'peso_entrada'),1)} kg`:'—'} detail="Peso de entrada informado"/><Stat label="Custo por cabeça" value={active.length&&transactions.some(x=>x.tipo==='despesa')?money(sum(transactions.filter(x=>x.tipo==='despesa'),'valor')/active.length):'—'} detail="Despesas registradas / animais"/></div><Card title="Lotes"><p className="section-note">Organize os animais e acompanhe metas de peso.</p>{lots.length?lots.map(x=><Link className="lot-row" key={String(x.id)} href={`/lotes/${x.id}`}><div><strong>{String(x.nome)}</strong><small>{active.filter(a=>a.lote_id===x.id).length} animais · {x.categoria||'Sem categoria'}</small></div><span>Ver detalhes →</span></Link>):<Empty title="Ainda não há lotes" description="Crie seu primeiro lote e acompanhe o desempenho ao longo do tempo."/>}<Link className="module-action" href="/painel/novo/lote"><Icon name="plus" size={17}/> Criar lote</Link></Card><Card title="Animais individuais">{animals.length?animals.map(x=><Link className="data-row" href={`/animais/${x.id}`} key={String(x.id)}><div><strong>{String(x.nome||x.identificacao)}</strong><small>{String(x.identificacao)} · {String(x.categoria||'Corte')} · {String(x.status)}</small></div><span>{x.peso_entrada!=null?`${number(Number(x.peso_entrada),1)} kg`:'Sem peso'} →</span></Link>):<Empty title="Nenhum animal registrado" description="O cadastro de animais aparecerá aqui."/>}<Link className="module-action" href="/painel/novo/corte"><Icon name="plus" size={17}/> Animal de corte</Link></Card>{weighings.length>0&&<Link className="text-link" href="/painel/pesagens">Ver pesagens →</Link>}</>);}
 if(area==='leite'){const [milk,animalsRaw]=await Promise.all([load('producao_leite','data_producao'),load('animais')]);const animals=animalsRaw.filter(isDairy);const {start,end,label}=monthBounds(),current=milk.filter(x=>String(x.data_producao)>=start&&String(x.data_producao)<end),liters=milkDailyTotals(current).reduce((total,day)=>total+day.liters,0),revenue=current.filter(x=>x.preco_litro!=null).reduce((a,x)=>a+Number(x.litros)*Number(x.preco_litro),0);const dairy=animals.filter(x=>x.status==='ativo');const daily=milkDailyTotals(current).map(x=>[x.day,x.liters] as [string,number]);return shell(<><section className="module-hero module-hero-milk"><span>ATIVIDADE LEITEIRA</span><h2>Do cuidado à produção.</h2><p>Suas vacas leiteiras e cada litro produzido, com clareza.</p><Link href="/painel/novo/leite"><Icon name="plus" size={18}/> Adicionar vaca leiteira</Link></section><div className="module-quick-actions"><Link href="/painel/novo/producao" className="module-action secondary"><Icon name="milk" size={18}/> Produção por vaca ou tanque</Link></div><p className="section-note">Atividade leiteira · {label}</p><div className="stat-grid"><Stat label="Vacas em lactação" value={dairy.filter(x=>String(x.situacao_leite||'').toLowerCase().includes('lact')).length||'—'}/><Stat label="Produção do mês" value={current.length?`${number(liters,1)} L`:'—'}/><Stat label="Média diária" value={daily.length?`${number(liters/daily.length,1)} L`:'—'}/><Stat label="Receita estimada" value={revenue?money(revenue):'—'}/></div><Card title="Produção diária"><TrendChart points={daily.slice(-20).map(([day,v])=>({label:day.slice(8),value:v}))} unit="L"/></Card><Card title="Produção por vaca"><Bars values={animals.map(a=>sum(current.filter(x=>x.animal_id===a.id),'litros')).filter(v=>v>0)} labels={animals.filter(a=>current.some(x=>x.animal_id===a.id)).map(a=>String(a.nome||a.identificacao))}/></Card><Card title="Vacas leiteiras">{animals.length?animals.map(x=><Link href={`/animais/${x.id}`} className="data-row" key={String(x.id)}><div><strong>{String(x.nome||x.identificacao)}</strong><small>{String(x.identificacao)} · {String(x.raca||'Raça não informada')} · {String(x.situacao_leite||'Situação não informada')}</small></div><span>Detalhes →</span></Link>):<Empty title="Nenhuma vaca leiteira" description="Cadastre as vacas nesta área para mantê-las separadas do gado de corte."/>}<Link className="module-action" href="/painel/novo/leite"><Icon name="plus" size={17}/> Vaca leiteira</Link></Card><Card title="Histórico">{milk.length?milk.slice(0,30).map(x=><div className="data-row" key={String(x.id)}><div><strong>{number(Number(x.litros),1)} litros</strong><small>{date(String(x.data_producao))} · {x.animal_id?animals.find(a=>a.id===x.animal_id)?.nome||'Vaca':'Tanque/rebanho'}</small></div><span>{x.preco_litro!=null?money(Number(x.litros)*Number(x.preco_litro)):'—'}</span></div>):<Empty title="Sem produção registrada" description="Anote a produção para acompanhar o leite."/>}</Card></>);}
 if(area==='pesagens'){const [rows,lotsRaw,animalsRaw]=await Promise.all([load('pesagens','data_pesagem'),load('lotes'),load('animais')]);const lots=lotsRaw.filter(x=>String(x.sistema||'corte')==='corte');const animals=animalsRaw.filter(x=>!isDairy(x));const ordered=[...rows].sort((a,b)=>String(a.data_pesagem).localeCompare(String(b.data_pesagem)));const latest=rows[0],previous=rows[1];const days=latest&&previous?(new Date(String(latest.data_pesagem)).getTime()-new Date(String(previous.data_pesagem)).getTime())/86400000:0;const comparable=latest&&previous&&latest.lote_id===previous.lote_id&&latest.animal_id===previous.animal_id;const gmd=comparable&&days>0?(Number(latest.peso_kg)-Number(previous.peso_kg))/days:null;return shell(<><div className="stat-grid"><Stat label="Último peso" value={latest?`${number(Number(latest.peso_kg),1)} kg`:'—'}/><Stat label="Pesagens" value={rows.length}/><Stat label="GMD comparável" value={gmd!=null?`${number(gmd,2)} kg/dia`:'—'} detail="Duas pesagens do mesmo animal/lote"/></div><Card title="Evolução de peso"><TrendChart points={ordered.slice(-16).map(x=>({label:date(String(x.data_pesagem)).slice(0,5),value:Number(x.peso_kg)}))} unit="kg"/></Card><Card title="Histórico">{rows.length?rows.map(x=><div className="data-row" key={String(x.id)}><div><strong>{number(Number(x.peso_kg),1)} kg</strong><small>{date(String(x.data_pesagem))} · {lots.find(l=>l.id===x.lote_id)?.nome||animals.find(a=>a.id===x.animal_id)?.identificacao||'Sem vínculo'}</small></div><span>{x.quantidade_animais?`${x.quantidade_animais} animais`:'Individual'}</span></div>):<Empty title="Sem pesagens" description="Registre o peso de um lote ou animal para visualizar a evolução."/>}</Card><Card title="Nova pesagem"><Link className="module-action" href="/painel/novo/pesagem"><Icon name="plus" size={17}/> Nova pesagem</Link></Card></>);}
 if(area==='estoque'){const stock=await load('estoque');const low=stock.filter(x=>x.ativo&&x.estoque_minimo!=null&&Number(x.quantidade_atual)<=Number(x.estoque_minimo));return shell(<><div className="stat-grid"><Stat label="Itens ativos" value={stock.filter(x=>x.ativo).length}/><Stat label="Estoque baixo" value={low.length} tone={low.length?'negative':''}/></div><Card title="Insumos e materiais">{stock.length?stock.map(x=>{const days=Number(x.consumo_medio_dia)>0?Number(x.quantidade_atual)/Number(x.consumo_medio_dia):null;const isLow=low.some(y=>y.id===x.id);return <div className="data-row" key={String(x.id)}><div><strong>{String(x.nome)} {isLow&&<em className="low-tag">Estoque baixo</em>}</strong><small>{x.categoria||'Insumo'} · consumo {Number(x.consumo_medio_dia)>0?`${number(Number(x.consumo_medio_dia),1)} ${x.unidade}/dia`:'não informado'}</small></div><span>{number(Number(x.quantidade_atual),1)} {String(x.unidade)}<small>{days!=null?`~ ${number(days,0)} dias restantes`:'Sem previsão'}</small></span></div>}):<Empty title="Estoque vazio" description="Cadastre ração, sal, silagem, medicamentos, combustível e outros itens."/>}</Card><Card title="Novo item"><Link className="module-action" href="/painel/novo/estoque"><Icon name="plus" size={17}/> Novo item</Link></Card></>);}
 if(area==='assistente'){const [animals,lots,transactions,milk,stock]=await Promise.all([load('animais'),load('lotes'),load('transacoes'),load('producao_leite'),load('estoque')]);const {start,end}=monthBounds(),tx=transactions.filter(x=>String(x.data_competencia)>=start&&String(x.data_competencia)<end),lm=milk.filter(x=>String(x.data_producao)>=start&&String(x.data_producao)<end);return shell(<><Card title="Pergunte sobre sua fazenda"><p className="section-note">Respostas calculadas dos registros reais. Perguntas abertas que dependam de análise generativa exigem um serviço de IA configurado no servidor.</p><AssistantQuestions facts={{financial:tx.length?`Resultado de ${money(sum(tx.filter(x=>x.tipo==='receita'),'valor')-sum(tx.filter(x=>x.tipo==='despesa'),'valor'))}, calculado a partir de ${tx.length} lançamentos neste mês.`:'Ainda não há lançamentos financeiros neste mês.',herd:`${animals.filter(x=>x.status==='ativo').length} animais ativos e ${lots.filter(x=>x.ativo).length} lotes ativos.`,milk:lm.length?`Foram registrados ${number(milkDailyTotals(lm).reduce((total,day)=>total+day.liters,0),1)} litros neste mês, em ${lm.length} lançamentos.`:'Ainda não há produção de leite registrada neste mês.',stock:stock.filter(x=>x.ativo&&x.estoque_minimo!=null&&Number(x.quantidade_atual)<=Number(x.estoque_minimo)).map(x=>String(x.nome)).join(', ')||'Nenhum item com estoque baixo nos registros atuais.'}}/></Card></>);}
 notFound();
}
